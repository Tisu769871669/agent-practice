id: "002"
slug: "json-only"
title: "JSON Only"
track: "foundations"
difficulty: "easy"
status: "runnable"
version: "0.1.0"
summary: "Extract a task into a strict JSON object."
tags:
  - structured-output
  - extraction
frameworks:
  official:
    - raw-python
    - langchain
    - langgraph
  interface: "framework-neutral"
entrypoint:
  module: "solution.agent"
  callable: "run"
  signature: "run(input: dict, context: AgentContext) -> dict"
schemas:
  input: "schemas/input.schema.json"
  output: "schemas/output.schema.json"
fixtures:
  public: "fixtures/public.jsonl"
limits:
  timeout_seconds: 60
  max_tool_calls: 0
  max_output_chars: 2000
scoring:
  max_score: 100
  pass_score: 90
  metrics:
    - name: "schema_validity"
      weight: 20
    - name: "field_extraction"
      weight: 60
    - name: "null_date_handling"
      weight: 20
