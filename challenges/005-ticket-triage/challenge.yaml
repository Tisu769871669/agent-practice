id: "005"
slug: "ticket-triage"
title: "Ticket Triage"
track: "foundations"
difficulty: "easy"
status: "runnable"
version: "0.1.0"
summary: "Classify support tickets with stable labels."
tags:
  - classification
  - support
frameworks:
  official:
    - raw-python
    - langchain
    - langgraph
  interface: "framework-neutral"
entrypoint:
  module: "solution.agent"
  callable: "run"
  signature: "run(input: dict, context: AgentContext) -> dict"
schemas:
  input: "schemas/input.schema.json"
  output: "schemas/output.schema.json"
fixtures:
  public: "fixtures/public.jsonl"
limits:
  timeout_seconds: 60
  max_tool_calls: 0
  max_output_chars: 2000
scoring:
  max_score: 100
  pass_score: 90
  metrics:
    - name: "schema_validity"
      weight: 20
    - name: "category_label"
      weight: 40
    - name: "severity_label"
      weight: 40
