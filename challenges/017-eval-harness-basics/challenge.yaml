id: "017"
slug: "eval-harness-basics"
title: "Eval Harness Basics"
track: "evaluation"
difficulty: "medium"
status: "runnable"
version: "0.1.0"
summary: "Inspect a simple deterministic grader and eval harness."
tags:
  - evaluation
  - grading
frameworks:
  official:
    - raw-python
    - langchain
    - langgraph
  interface: "framework-neutral"
entrypoint:
  module: "solution.agent"
  callable: "run"
  signature: "run(input: dict, context: AgentContext) -> dict"
schemas:
  input: "schemas/input.schema.json"
  output: "schemas/output.schema.json"
fixtures:
  public: "fixtures/public.jsonl"
limits:
  timeout_seconds: 60
  max_tool_calls: 0
  max_output_chars: 3000
scoring:
  max_score: 100
  pass_score: 90
  metrics:
    - name: "schema_validity"
      weight: 20
    - name: "check_results"
      weight: 40
    - name: "passed_flag"
      weight: 20
    - name: "score_value"
      weight: 20
