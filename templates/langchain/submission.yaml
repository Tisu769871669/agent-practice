schema_version: "0.1"
submission_id: "local"
template: "langchain"
entrypoint:
  module: "solution.agent"
  callable: "run"
  signature: "run(input: dict, context: AgentContext) -> dict"
metadata:
  submission_name: "LangChain starter"
