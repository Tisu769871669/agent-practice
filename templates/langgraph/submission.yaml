schema_version: "0.1"
submission_id: "local"
template: "langgraph"
entrypoint:
  module: "solution.agent"
  callable: "run"
  signature: "run(input: dict, context: AgentContext) -> dict"
metadata:
  submission_name: "LangGraph starter"
